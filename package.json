{
  "name": "restaurant-backend",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "shared"
  ],
  "scripts": {
    "start:dev": "concurrently --kill-others \"npm run dev --workspaces\"",
    "test:all": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^7.0.0",
    "nodemon": "^2.0.20"
  }
}
